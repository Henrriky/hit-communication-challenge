type InputState = {
    value: string,
    validity: boolean,
  }
  
  function Form() {
  
    const { token, setToken } = useContext(AuthContext);
  
    const [password, setPassword] = useState<InputState>({value: "", validity: false});
    const [email, setEmail] = useState<InputState>({value: "", validity: false});
    
    const handleEmailChange = (event: ChangeEvent<HTMLInputElement>) => {
      setEmail({value: event.target.value, validity: event.target.validity.valid});
    };
  
    const handlePasswordChange = (event: ChangeEvent<HTMLInputElement>) => {
      setPassword({value: event.target.value, validity: event.target.validity.valid});
    };
  
    function handleSubmit (event: React.FormEvent<HTMLFormElement>) {
        event.preventDefault();
  
        console.log(password.validity)
  
        
    }